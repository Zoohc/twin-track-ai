SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
ENCRYPTION_KEY=              # Fernet 32바이트 base64 (python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
ALLOWED_ORIGINS=http://localhost:3000
